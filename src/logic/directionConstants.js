// MOVEMENT DIRECTIONS
const RIGHT = { x: 1, y: 0 };
const DOWN = { x: 0, y: 1 };
const LEFT = { x: -1, y: 0 };

// ROTATION DIRECTIONS
const ROTATE_RIGHT = { angle: 90 };
const ROTATE_LEFT = { angle: -90 };

// AXES
export const X = "x";
export const Y = "y";

export const DIRECTIONS = { RIGHT, DOWN, LEFT, ROTATE_RIGHT, ROTATE_LEFT };
